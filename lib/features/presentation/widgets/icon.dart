import 'package:flutter/material.dart';

class HappyIcon extends StatelessWidget {
  const HappyIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.tag_faces, color: Colors.orange, size: 60);
  }
}
